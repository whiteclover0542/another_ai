# 인계 문서 (HANDOFF)

> 이 문서만 보고 이어서 작업할 수 있어야 합니다. 이전 대화 전문은 없다고 가정하고 작성했습니다.
> 배경·검사 10개·과제 규칙의 전체 원문은 [ASSIGNMENT.md](ASSIGNMENT.md)와 [PROGRESS.md](PROGRESS.md)에 있습니다.

## 1. 목표

정적 사이트 "오늘의 CVE 정보판"의 "🎯 오늘의 대표 CVE" 카드에 **심각도 필터**를 추가한다.

> **사용자가 심각도 필터 버튼(전체/심각/높음/중간/낮음)을 클릭하면 대표 CVE 목록이 그 심각도 항목만 보이도록 즉시 바뀐다.**

- 필터는 대표 CVE 목록에만 적용되고, 심각도 분포 도넛 차트·범례는 항상 전체 분포를 그대로 보여줘야 한다(별개 기능).
- 필터 상태는 세션 메모리(JS 변수)에만 두고 새로고침하면 초기화되어야 한다. localStorage나 서버 저장 금지.

## 2. 현재 상태

- 기능은 구현되어 배포까지 완료됨: https://whiteclover0542.github.io/another_ai/
- 오너가 실제 배포 페이지에서 필터 버튼을 눌러 골든 패스(전체 → 특정 심각도 클릭 → 목록 즉시 변경 → 전체 복원)는 **정상 작동을 직접 확인**했다.
- 그 외 4개 검사(4, 5, 6, 7, 10 — 아래 5번 참고)는 아직 실제 브라우저로 확인되지 않았다. 코드 상으로는 처리되어 있다고 판단하지만 실제로 눌러서 확인이 필요하다.
- 변경된 파일: `index.html`(필터 컨테이너 `#highlights-filter` 추가), `assets/app.js`(`setHighlightFilter`/`renderHighlightsFilter`/`renderHighlightsList`/`SEVERITY_LEVELS_FOR_HIGHLIGHTS`/`HIGHLIGHT_FILTER_VALUES` 추가, 기존 `renderHighlights`를 필터 경유하도록 수정), `assets/style.css`(`.highlights-filter`/`.hl-filter-btn`/`.highlights-empty` 스타일 추가).
- 관련 최신 커밋: `fd2c5a2` ("feat: 대표 CVE 목록에 심각도 필터 추가")

## 3. 실행 명령

빌드 단계 없는 정적 사이트(순수 HTML/CSS/JS, npm 설치 불필요).

```bash
# 저장소 루트에서 아무 정적 서버로 열면 됨. 예:
npx serve .
# 또는 Python이 있다면:
python -m http.server 8000
```

브라우저에서 `http://localhost:8000/` (또는 `npx serve` 출력 주소) 접속. 장애 시연 패널을 보려면 `?debug=1` 쿼리를 붙인다: `http://localhost:8000/?debug=1`.

배포본은 push 후 GitHub Pages가 자동 반영한다(별도 빌드 명령 없음). 로컬 확인이 배포본과 다르면 브라우저 강력 새로고침(캐시 무시)으로 재확인할 것 — `index.html`의 `?v=21` 쿼리로 캐시버스팅 중이며, 다음 수정 시 이 숫자를 올려야 한다.

## 4. 통과한 검사 (10개 중)

원문 검사 목록은 [PROGRESS.md의 "검사 10개" 절](PROGRESS.md)에 있다. 여기서는 상태만 요약한다.

| # | 검사 | 상태 |
|---|---|---|
| 1 | 첫 로드 시 전체 항목 + "전체" 선택 표시 | ✅ 실제 클릭으로 확인 |
| 2 | "높음" 클릭 → HIGH만 남고 개수 즉시 변경 | ✅ 실제 클릭으로 확인 |
| 3 | 다시 "전체" 클릭 → 전체 복원 | ✅ 실제 클릭으로 확인 |
| 4 | 필터 변경 후 새로고침 → "전체"로 초기화 | ⬜ 코드상 자명(세션 변수라 새로고침 시 리셋)하나 미확인 |
| 5 | 클릭한 심각도에 대표 CVE 0건 → "해당 심각도의 대표 CVE 없음" 표시(위조 금지) | ⬜ 미확인 |
| 6 | `?debug=1` API 실패 재현 상태에서 필터 클릭해도 콘솔 에러 없이 동작 | ⬜ 미확인 |
| 7 | 잘못된 필터 값이 들어와도 "전체"로 안전 처리 | ⬜ 미확인 |
| 8 | 도넛 차트·범례는 필터와 무관하게 항상 전체 분포 표시 | ⬜ 코드상 분리돼 있음(별도 함수 `renderSeverity`, 필터 미적용)하나 미확인 |
| 9 | 출처 링크·조회 시각·어제 대비 비교 카드 등 기존 기능 정상 | ⬜ 미확인 |
| 10 | `?debug=1` 장애 5종 버튼 + 마지막 정상값 유지가 필터 추가 후에도 동일 | ⬜ 미확인 |

## 5. 남은 문제

1. **위 표의 4, 5, 6, 7, 8, 9, 10번 검사를 실제 브라우저로 확인**하는 것이 가장 큰 남은 일이다. 특히:
   - 5번: 오늘(2026-08-24 KST) 기록의 대표 CVE 3건이 전부 `HIGH`다. "심각"이나 "중간"이나 "낮음"을 누르면 5번 검사를 바로 확인할 수 있다.
   - 7번: 브라우저 개발자 도구 콘솔에서 `setHighlightFilter('nonsense')`를 직접 실행해보면 된다(`app.js`는 모듈이 아닌 일반 스크립트라 이 함수가 전역에 노출돼 있다).
2. 확인하다가 실패하는 검사가 있으면, 그 검사 번호와 실제로 무엇이 잘못됐는지(에러 메시지, 스크린샷, 콘솔 로그)를 이 문서와 `PROGRESS.md`의 카드 4/5 절에 기록할 것.
3. `PROGRESS.md`에 시간(분) 단위 기록이 비어 있다 — 이번 작업부터는 시작 시각을 먼저 적어두고 진행할 것(과제 규칙상 두 AI를 같은 단위로 비교해야 함).

## 6. 다음 행동

1. 저장소를 로컬에 두고(또는 배포 주소로 바로) 위 "실행 명령"대로 열어본다.
2. "통과한 검사" 표에서 ⬜인 4, 5, 6, 7, 8, 9, 10번을 순서대로 실제로 눌러/재현해보며 확인한다.
3. 전부 통과하면 `PROGRESS.md`의 카드 4(AI B 작업 로그, 요청 수 상한 12회)와 카드 5(두 AI 비교표·최종 검사 10개 재실행)를 채운다.
4. 실패하는 검사가 있으면 최소한으로 고치고, 고친 뒤 관련 검사뿐 아니라 이미 통과했던 1~3번도 다시 눌러 회귀가 없는지 확인한다(기존 기능을 깨뜨리지 않는 것이 중요).
5. 모두 끝나면 커밋 메시지에 어떤 검사를 확인했는지 남기고 push한다.

## 7. 건드리면 안 되는 부분

- **원본 today_information 저장소/배포**(`github.com/whiteclover0542/today_information`, `whiteclover0542.github.io/today_information`)는 이번 과제와 무관하니 절대 수정하지 않는다. 이 저장소(`another_ai`)에만 작업한다.
- `assets/app.js`의 `renderSeverity()`(도넛 차트·범례)는 이번 필터 기능과 **분리되어야 한다** — 필터가 그 함수 안으로 침투해서 전체 분포 표시를 깨뜨리면 안 된다(검사 8).
- `data/history.json`은 GitHub Actions(`daily-cve-count.yml`)가 매일 자동으로 추가하는 실제 기록이다. 손으로 값을 수정하거나 위조하지 않는다.
- `?debug=1` 장애 시연 로직(`fetchHistory`의 `simulateKind` 분기)은 과제 4에서 이미 검증된 기존 기능이다 — 필터 추가 때문에 이 로직을 변경할 필요는 없다. 건드리지 말 것.
- API 키/비밀값을 새로 추가하지 않는다(원래 앱이 무키 경로만 쓰는 것이 설계 원칙).
- `ASSIGNMENT.md`는 동결 문서다. 수정하지 않는다.
