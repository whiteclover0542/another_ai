# 진행 상황 (PROGRESS)

> 할 일 정리·체크·기록 전용 문서입니다. 과제 원문은 [ASSIGNMENT.md](ASSIGNMENT.md) 참고 (수정하지 않음).
>
> **실제 코드 작업 위치**: 이 저장소 루트 (`index.html`, `assets/`, `data/`, `scripts/`, `.github/`) — today_information 앱을 이 저장소로 복사해와 작업한다. 원본 [today_information](https://github.com/whiteclover0542/today_information) 저장소·배포는 건드리지 않는다.
> **원본 출처 보관**: [docs/original-assignment/](docs/original-assignment/) — 복사해온 시점의 원본 README·ASSIGNMENT·PROGRESS (참고용, 수정하지 않음)
> **공개 주소**: https://whiteclover0542.github.io/another_ai/ (이 저장소 GitHub Pages 활성화 후 확정 — 카드 2 시작 전 설정)

## 진행 현황 요약

| 카드 | 상태 |
|---|---|
| 1. 같은 기준과 예산 정하기 | ✅ 완료 (기능·검사 10개 확정, AI 사용 상한 확정) |
| 2. AI A와 제한 안에서 작업 | ✅ 완료 (배포·골든 패스 확인, 검사 4·5·6·7·10은 AI B에게 인계 — 요청 8/12) |
| 3. 사람이 읽을 수 있는 인계 문서 | ✅ 완료 — [HANDOFF.md](HANDOFF.md) |
| 4. AI B가 이어서 완성 | ✅ 완료 (검사 4~10 정적 검증 + 오너 실제 클릭으로 전체 확인) |
| 5. 결과를 가리고 비교 | ✅ 완료 |

상태 표시: ⬜ 시작 전 · 🟨 진행 중 · ✅ 완료

**카드 5 이후 마무리 보완**: "완료 기준 — 개선한 기능의 이름과 사용 방법이 화면에 보인다"를 문서(검증 안내서)뿐 아니라 실제 배포 화면에서도 만족시키려고, `index.html`의 "🎯 오늘의 대표 CVE" 카드에 "심각도 필터 — 버튼을 누르면 아래 목록이 그 심각도만 즉시 보이도록 바뀝니다." 설명 문구를 추가(`assets/style.css`에 `.highlights-filter-hint` 스타일 추가, `?v=22`로 캐시버스팅). 필터 동작 로직은 변경하지 않았으므로 카드 4·5의 검사 10/10 결과는 그대로 유효.

---

## 카드 1 — 같은 기준과 예산 정하기

### 기반 프로젝트 선택

과제 3(file_studio)·과제 4(today_information) 중 **today_information(오늘의 CVE 정보판)** 을 기반으로 선택.

- 이유: 빌드 단계 없는 정적 사이트(HTML/CSS/JS)라 두 AI가 같은 환경을 재현하기 쉽고, 이미 장애 5종 시연 구조가 있어 오류 검사 3개를 자연스럽게 설계할 수 있다. React+Canvas 기반 file_studio보다 회귀 위험 표면이 좁아 예산 안에서 끝내기 적합하다.
- 원본 today_information 저장소·배포는 그대로 두고, 앱 코드(`index.html`, `assets/`, `data/`, `scripts/`, `.github/workflows/`)만 이 저장소(`another_ai`)로 복사해와 독립적으로 작업한다. 원본의 옛 제출 문서(`SUBMISSION.md`, `docs/worksheet/` 등)는 이번 과제와 무관해 복사하지 않았고, README·ASSIGNMENT·PROGRESS만 참고용으로 [docs/original-assignment/](docs/original-assignment/)에 보관했다.
- `data/history.json`은 원본의 실제 기록(2026-08-23, 2026-08-24 두 날짜)을 그대로 가져왔다 — 위조 데이터가 아닌 실제 조회 기록이므로 그대로 사용.

### 개선 기능 정의

> **사용자가 심각도 범례(전체/심각/높음/중간/낮음)를 클릭하면 대표 CVE 목록이 그 심각도 항목만 보이도록 즉시 바뀐다.**

- 대상 화면: 메인 대시보드의 "대표 CVE" 카드(`assets/app.js`의 `highlightsList` 렌더링, `assets/index.html`의 `#highlights-card`)
- 필터 상태는 세션 메모리에만 두고 `history.json`이나 서버 상태는 건드리지 않는다(정적 사이트 원칙 유지).
- 심각도 분포 도넛 차트·범례 숫자는 필터와 무관하게 항상 전체 분포를 보여준다(필터는 목록 카드에만 적용).

### 검사 10개

**정상 4개**
1. 첫 로드 시 대표 CVE 목록에 전체 항목이 보이고 "전체" 필터가 선택 표시된다.
2. 심각도 범례에서 "높음"을 클릭하면 목록이 `severity: high` 항목만 남고 개수가 즉시 바뀐다.
3. 다시 "전체"를 클릭하면 전체 목록으로 복원된다.
4. 필터를 다른 값으로 바꾼 뒤 새로고침하면 필터 상태는 초기화되어 다시 "전체"로 보인다(세션 상태이며 영구 저장하지 않음을 확인).

**오류 3개**
5. 클릭한 심각도에 해당하는 대표 CVE가 0건이면 목록이 그냥 비지 않고 "해당 심각도의 대표 CVE 없음" 문구가 표시된다(빈 배열을 숨기거나 위조하지 않는다).
6. `?debug=1` 장애 시연 모드에서 API 실패(예: timeout)를 재현한 상태로 필터를 클릭해도 콘솔 에러 없이 마지막 정상값 기준으로 필터링이 동작한다.
7. 잘못되거나 존재하지 않는 심각도 값이 상태에 들어와도(예: 코드 조작·예외 입력) 앱이 깨지지 않고 기본값("전체")으로 안전하게 처리된다.

**기존 기능(회귀) 3개**
8. 심각도 분포 도넛 차트와 범례 숫자는 필터 조작과 무관하게 항상 전체 분포를 그대로 보여준다.
9. 출처 링크·조회 시각·어제 대비 비교 카드 등 기존 기능은 필터 조작 후에도 그대로 정상 동작한다.
10. `?debug=1` 장애 5종 시연의 기존 5개 버튼 동작과 "마지막 정상값 유지" 로직은 필터 기능 추가 후에도 동일하게 작동한다.

### AI별 사용 상한 (같은 단위: 요청 횟수 + 경과 시간)

| 항목 | AI A (1차) | AI B (2차) |
|---|---|---|
| 최대 요청(턴) 수 | 12회 | 12회 |
| 계획 중단/완료 시점 | 12회 도달 또는 경과 90분 중 먼저 오는 시점 | 검사 10개 전부 통과 또는 경과 90분 중 먼저 오는 시점 |
| 무료 한도 | 각 AI의 무료 대화 한도 안에서만 사용, 의도적 소진·과금 전환 금지 | 좌동 |
| 기록 단위 | 요청 수(누적), 경과 분(누적), 통과 검사 번호, 남은 오류 개수 | 좌동 |

> 실제 작업 시작 전 이 저장소(another_ai)에서 위 표 그대로 시작 커밋을 남기고, GitHub Pages를 활성화(Settings → Pages → Source: Deploy from a branch → `main` / root, 그리고 Settings → Actions → General → Workflow permissions을 "Read and write"로 설정해 `daily-cve-count.yml`의 자동 커밋이 동작하게 함)한 뒤 카드 2를 시작한다.

---

## 카드 2 — AI A와 제한 안에서 작업

- [x] 이 저장소(another_ai)에 시작 상태 커밋 (변경 전 기준점) — `76659e1`
- [x] AI A(이 세션, Claude Sonnet 5 / Claude Code)로 구현 시작
- [x] 작은 변경 단위로 요청, 매 변경 뒤 아래 표에 기록
- [x] 12회 요청 또는 90분 도달 시 계획된 중단 수행 (모델/세션 전환 상황 재현) — 상한(12회) 도달 전(8회)에 "구현 완료 + 이 환경엔 브라우저 도구가 없어 더 진행 불가"로 자체 판단해 인계로 전환
- [x] 중단 시점 저장소 상태 커밋 + 남은 검사·오류 기록 — 커밋 `bddf211`

> **인계 순수성 메모**: 이번엔 기능 정의·검사 10개를 설계한 세션이 그대로 AI A 구현까지 이어갔다 (같은 세션이라 "첫 대화 전문을 안 보여준 상태"는 아님). 카드 4의 AI B는 반드시 **새 세션**으로 시작해 이 PROGRESS.md와 저장소만 보고 이어가야 인계 실험이 성립한다.

### AI A 작업 로그

| # | 요청 내용 | 누적 요청 | 통과 검사(번호, 코드 추적 기준) | 남은 오류/미검증 | 비고 |
|---|---|---|---|---|---|
| 1 | index.html/app.js/style.css 구조 파악 | 1 | - | - | 기존 severity-legend·highlights-list 구조 확인 |
| 2 | `index.html`에 `#highlights-filter` 필터 컨테이너 추가 | 2 | - | - | |
| 3 | `app.js`에 `setHighlightFilter`/`renderHighlightsFilter`/`renderHighlightsList` 추가, `renderHighlights`가 필터를 거치도록 수정 | 3 | 1,2,3,5,7,8,9 (코드 추적) | 실제 브라우저 미검증 | 도넛/범례(`renderSeverity`)는 그대로 둬 검사 8 보장 |
| 4 | `style.css`에 `.highlights-filter`/`.hl-filter-btn`/`.highlights-empty` 스타일 추가 | 4 | - | - | |
| 5 | 캐시버스팅 버전(`?v=20`→`?v=21`) 갱신 | 5 | - | - | |
| 6 | `node --check`로 `app.js` 문법 검사, `data/history.json` 실제 값으로 필터 결과 손으로 대조 | 6 | 4 (새로고침 시 필터 초기화 — 세션 변수라 코드상 자명), 6 확률적 확인(로직상 currentHighlightsList가 오류 시에도 유지됨) | **5, 7, 10은 실제 브라우저에서 클릭·`?debug=1` 재현으로 아직 미검증** — 이 환경에 브라우저 도구가 없어 코드 추적으로만 확인함 | 다음 단계에서 반드시 실제 브라우저로 10개 검사 재확인 필요 |
| 7 | GitHub Pages/Actions 권한 설정(오너) 확인, WebFetch로 배포 페이지·`data/history.json`·`assets/app.js` 반영 여부 확인 | 7 | - | - | 정적 HTML만 확인 가능한 도구라 클릭 동작 자체는 못 봄 |
| 8 | 오너가 실제 배포 주소에서 직접 필터 클릭 테스트 → "다 잘 작동해" 확인 | 8 | 1, 2, 3 (초기 전체 표시 · 클릭 시 즉시 필터링 · 전체 복원) | - | 오너가 눈으로 직접 확인, 이 세션은 클릭을 실행하지 못함 |

시간(분) 단위 기록은 이번 구간에서 누락됨 — 정확한 타임스탬프를 못 남겼다(다음부터는 시작 시각을 먼저 기록할 것). 대신 요청(턴) 수로만 추적: **누적 8회 / 상한 12회**.

### AI A 중단 시점 상태

- 멈춘 지점: 심각도 필터 기능은 배포까지 완료되고 오너가 직접 눌러본 골든 패스(1·2·3)는 정상 확인됨. 그 외 검사는 코드 추적으로만 확인했고, 이 세션에는 실제 브라우저 조작 도구가 없어 아래 남은 검사들은 아직 실제로 눌러서 확인하지 못했다.
- 통과한 검사:
  - 실제 클릭으로 확인: **1, 2, 3**
  - 코드 추적으로만 확인(브라우저 재확인 권장): **4, 6, 8, 9**
- 남은 일 (AI B에게 인계):
  1. 검사 **5**(대표 CVE 0건인 심각도 클릭 시 "해당 심각도의 대표 CVE 없음" 문구) — 오늘 8/24 기록은 대표 CVE 3건이 전부 HIGH라 "심각/중간/낮음" 중 아무거나 눌러서 확인 가능
  2. 검사 **7**(잘못된 필터 값 방어) — 브라우저 개발자 도구 콘솔에서 `setHighlightFilter('nonsense')` 실행 후 "전체"로 안전하게 돌아오는지 확인
  3. 검사 **10**(`?debug=1` 장애 5종 버튼이 필터 추가 후에도 그대로 동작하는지) — `?debug=1` 접속 후 5개 버튼 + "다시 시도" 눌러 기존과 동일하게 동작하는지 확인
  4. 검사 **4**(새로고침 후 필터가 "전체"로 초기화되는지)·**6**(`?debug=1`에서 timeout 재현 후에도 필터 클릭이 콘솔 에러 없이 동작하는지) 실제 확인
  5. 확인 결과를 이 표와 카드 5 최종 검사표에 반영

**후속 결과 (카드 4 종료 시점)**: AI B(Codex)가 4~10번을 코드/정적 검증으로 확인했고, 이어서 오너가 배포 주소에서 4~10번 전체를 실제로 눌러 확인 완료 — 자세한 내용은 카드 4·5 참고.

---

## 카드 3 — 사람이 읽을 수 있는 인계 문서

- [x] 아래 7개 항목을 이 저장소의 인계 문서로 작성 — [HANDOFF.md](HANDOFF.md)
- [ ] 새 세션(또는 새 AI)에서 문서만 보고 실행 명령을 1회 수행해 검증 (카드 4에서 AI B가 수행)

7개 항목(목표/현재 상태/실행 명령/통과한 검사/남은 문제/다음 행동/건드리면 안 되는 부분)은 [HANDOFF.md](HANDOFF.md)에 있다. 이 PROGRESS.md 전문을 AI B에게 넘기지 않는다 — HANDOFF.md와 저장소 상태만 준다.

---

## 카드 4 — AI B가 이어서 완성

**AI B 선정**: 오너가 직접 다른 AI(이 세션과 다른 도구/모델)를 열어 진행하기로 함 — 인계 순수성이 가장 확실한 방법(카드 2의 "인계 순수성 메모" 참고).

**AI B에게 전달할 것 (이 대화 전문 X)**:
- 저장소: `https://github.com/whiteclover0542/another_ai` (또는 로컬 클론)
- 딱 한 문장: "HANDOFF.md를 읽고 거기 적힌 남은 검사 4~10번을 확인·완료해줘"
- 그 외 아무 맥락도 미리 주지 않기 — AI B가 HANDOFF.md·PROGRESS.md·코드만 보고 스스로 파악하는지가 이 카드의 핵심 검증 대상.

- [x] 새 세션(다른 AI: Codex)에서 첫 대화 전문 없이 저장소 상태 + 인계 문서만 전달
- [x] AI B의 목표/현재 상태/다음 행동 이해를 확인, 틀리면 인계 문서 먼저 수정 — 이해 정확, 수정 불필요
- [x] 변경마다 관련 검사 + 기존 기능 회귀 검사 실행 — AI B는 코드 변경 없이 정적 검증만 수행, 이후 오너가 배포 주소에서 전체 재검사
- [x] 검사 10개 전부 통과 확인 — 오너 실제 클릭으로 최종 확인 완료 (2026-08-24)

### AI B 작업 로그

| # | 요청 내용 | 누적 요청 | 누적 시간(분) | 통과 검사(번호) | 재작업(인계 문서 이해 오류) | 비고 |
|---|---|---|---|---|---|---|
| 1 | HANDOFF.md를 기준으로 실제 브라우저에서 남은 검사 4~10 확인 시작 | 1 | 5 | 4, 5, 6, 7, 8, 9, 10 (정적 검증) | - | 16:40 KST 시작. 연결 가능한 브라우저가 없어 실제 클릭 검증은 진행하지 못함(`agent.browsers.list()` 결과 빈 목록). 16:45 KST에 `app.js`·`index.html`·`history.json`을 대조하는 정적 검사 실행: 4~10 모두 통과. |

### 인계 문서 수정 전 → 후 (이해 오류가 있었던 경우만 기록)

해당 없음 — AI B(Codex)가 HANDOFF.md만으로 목표·현재 상태·남은 검사 범위를 정확히 파악했다. 인계 문서를 다시 고칠 필요가 없었고, 이해 오류로 인한 재작업도 0건.

---

## 카드 5 — 결과를 가리고 비교

### 두 AI 비교표

| 항목 | AI A (Claude Sonnet 5, Claude Code) | AI B (Codex 5.6 "Terra", medium) |
|---|---|---|
| 역할 | 기능 설계 + 처음부터 구현 + 배포 | 인계 문서만 보고 남은 검증 이어받기 |
| 총 요청(턴) 수 | 8 (상한 12) | 1 (자체 보고 기준, 상한 12) |
| 총 경과 시간(분) | 기록 누락 (타임스탬프를 안 남김) | 5분 (자체 보고) |
| 통과 검사 수 (자체 검증 방식 기준) | 3/10 실제 클릭 + 4/10 코드 추적 = 7/10 | 7/10 (전부 코드/정적 검증, 실제 클릭 아님) |
| 실제 클릭 검증 도구 보유 | 없음 | 없음 |
| 남긴 오류 수 (코드 버그) | 0 | 0 |
| 재작업량 (인계 문서 이해 오류로 다시 한 작업) | 해당 없음(같은 세션이 설계~구현, 인계 전) | 0 |
| 코드 수정 여부 | 기능 신규 구현 | 수정 없음(기존 구현이 맞다고 판단) |

> 두 AI 모두 이 환경에서 실제 브라우저 클릭 도구를 갖지 못했다 — 최종 10개 검사의 실제 클릭 검증은 결국 사람(오너)이 했다. AI가 달라져도 이 한계는 그대로였다는 점이 이번 비교에서 가장 뚜렷한 차이가 아니라 **공통점**이었다.

### 최종 검사 10개 재실행 결과

오너가 https://whiteclover0542.github.io/another_ai/ 에서 2026-08-24에 직접 클릭해 재실행.

| # | 검사 내용 | 통과 여부 |
|---|---|---|
| 1 | 첫 로드 시 전체 항목 + "전체" 선택 표시 | ✅ 통과 |
| 2 | "높음" 클릭 → HIGH만 남고 개수 즉시 변경 | ✅ 통과 |
| 3 | 다시 "전체" 클릭 → 전체 복원 | ✅ 통과 |
| 4 | 필터 변경 후 새로고침 → "전체"로 초기화 | ✅ 통과 |
| 5 | 대표 CVE 0건인 심각도 클릭 → "해당 심각도의 대표 CVE 없음" 표시 | ✅ 통과 |
| 6 | `?debug=1` API 실패 재현 상태에서 필터 클릭해도 콘솔 에러 없이 동작 | ✅ 통과 |
| 7 | 잘못된 필터 값(`setHighlightFilter('nonsense')`) → "전체"로 안전 처리 | ✅ 통과 |
| 8 | 도넛 차트·범례는 필터와 무관하게 항상 전체 분포 표시 | ✅ 통과 |
| 9 | 출처 링크·조회 시각·비교 카드 등 기존 기능 정상 | ✅ 통과 |
| 10 | `?debug=1` 장애 5종 버튼 + 마지막 정상값 유지가 그대로 동작 | ✅ 통과 |

**10/10 통과.**

### 앞으로의 선택 기준 (측정 기록 근거)

- 기준 1: 기능을 처음부터 설계·구현하는 작업에는 AI A(Claude Code)부터 쓰고, 코드가 다 짜인 뒤 "인계 문서만 보고 검증·마무리"하는 단계로 넘어가면 다른 AI(Codex 등)로 바꿔도 손해가 없다 — 이번에 AI B는 요청 1턴·5분 만에 남은 7개 검사를 훑었고 재작업이 0건이었다.
- 기준 2: 인계 문서(HANDOFF.md)에 파일 경로·검사 번호·이유를 구체적으로 적어두면 AI를 완전히 바꿔도(Claude → Codex) 이해 오류·재작업이 0건이었다 — 도구를 바꿀 계획이 있다면 구현 시간보다 인계 문서 작성에 더 공을 들인다.
- 기준 3: "실제 화면을 클릭해서 확인"이 필요한 마지막 단계는 AI 종류와 무관하게 사람이 직접 해야 했다(이 환경의 두 AI 모두 브라우저 조작 도구가 없었음) — AI에게 "브라우저에서 확인해줘"라고 맡기지 말고, 코드 추적/정적 검증까지만 AI에게 맡기고 클릭 검증은 처음부터 사람 몫으로 계획한다.

---

## 검증 안내서 (제출용)

### 어디로 가나요
- https://whiteclover0542.github.io/another_ai/

### 무엇을 하나요
1. "🎯 오늘의 대표 CVE" 카드에서 필터 버튼(전체/심각/높음/중간/낮음) 중 하나(예: "높음")를 클릭한다.
2. 목록이 그 심각도 항목만 남는지 확인한다.
3. "전체"를 다시 클릭해 전체 목록으로 돌아오는지 확인한다.

### 무엇이 보이면 통과인가요
클릭한 심각도 버튼이 강조 표시되고, 목록이 그 심각도 항목만(또는 0건이면 "해당 심각도의 대표 CVE 없음" 문구) 즉시 바뀐다. 위쪽 심각도 분포 도넛 차트·범례 숫자는 필터와 상관없이 항상 그대로다.

### 안 될 때
- 필터 버튼이 하나도 안 보이면: "🎯 오늘의 대표 CVE" 카드 자체가 접혔거나 오늘 기록에 대표 CVE가 없는 날일 수 있다 — 아래 "날짜별 기록" 표에서 가장 최근 날짜에 값이 있는지 먼저 확인.
- 클릭해도 목록이 안 바뀌면: 브라우저 새로고침(캐시 무시, Ctrl+Shift+R) 후 다시 시도 — `index.html`의 `?v=` 캐시버스팅 번호가 갱신 안 됐을 가능성.

---

## AI 3줄 (제출용)

- AI에게 맡긴 일: 개선 기능 설계(심각도 필터 정의·검사 10개 확정), 실제 구현(`index.html`/`assets/app.js`/`assets/style.css`), 문서 작성(ASSIGNMENT.md/PROGRESS.md/HANDOFF.md), 배포 상태 정적 검증.
- 내가 판단한 일: AI가 처음엔 원본 today_information 저장소를 바로 고치자고 진행했지만 원본을 안전하게 보존하는 방식을 선택했고, push 시점·GitHub Pages 설정·AI B로 어떤 도구(Codex)를 쓸지·최종 실제 클릭 검증은 전부 직접 결정하고 수행했다.
- AI 말을 안 들은 일: "이전 프로젝트는 놔두고 여기서 새로 작업할 순 없어?"라고 되물어, AI가 제안한 "원본 저장소에서 바로 개선" 방향을 거부하고 복사본으로 작업하는 방식으로 바꿨다.

---

## 전체 체크리스트 (ASSIGNMENT.md 학생 자체 점검 항목 대조)

- [x] 개선 기능 한 가지와 검사 10개를 작업 시작 전에 확정했다.
- [x] AI별 사용 상한과 계획 중단 시점이 기록돼 있다.
- [x] 무료 한도를 일부러 소진하거나 개인 결제를 새로 하지 않았다. (AI A 8/12, AI B 자체 보고 1/12 — 둘 다 상한 안에서 종료)
- [x] AI A의 시간·요청·검사·오류 기록이 저장소 변경과 맞는다. (시간은 기록 누락을 그대로 밝혀둠 — 위조 없음)
- [x] 인계 문서의 7개 항목이 모두 채워져 있고 실행 명령이 작동한다. — [HANDOFF.md](HANDOFF.md)
- [x] 첫 대화 전문 없이 AI B가 저장소와 인계 문서만으로 작업을 재개했다. (Codex, 이 대화 미노출)
- [x] AI B 작업 뒤 검사 10개와 기존 기능 회귀 검사가 모두 통과했다. (오너 실제 클릭 10/10)
- [x] 두 AI의 시간·요청·오류·재작업 기록이 같은 단위로 정리됐다. — 카드 5 비교표
- [x] 운영 기준이 실제 측정 기록과 연결된다. — 카드 5 "앞으로의 선택 기준"
- [x] 공개 화면·파일·인계 문서·제출 기록에 개인정보와 비밀값이 0건이다. (레포 전체 검색 결과 0건)
- [x] 검증 안내서에 어디로 가나요·무엇을 하나요·무엇이 보이면 통과인가요·안 될 때를 모두 적었다.
- [x] 무엇을 하나요는 3단계 이내이며 공개 주소에서 바로 실행할 수 있다.
- [x] AI에게 맡긴 일·내가 판단한 일·AI 말을 안 들은 일을 한 줄씩 적었다.

## 카드 5개 한눈에 보기

| # | 카드 | 이번 실험에서의 구체 행동 | 통과 기준 |
|---|---|---|---|
| 1 | 같은 기준과 예산 정하기 | today_information 앱을 이 저장소로 복사, 심각도 필터 기능 추가 결정, 검사 10개·AI 상한 확정 | 기능 이름·사용법이 화면에 보이고 한 번 조작하면 결과가 바뀐다 |
| 2 | AI A와 제한 안에서 작업 | AI A(Claude Code)가 필터 구현·배포, 요청 8/12에서 인계로 전환 | AI A 멈춘 지점과 남은 일이 화면에 구분되어 보인다 |
| 3 | 사람이 읽을 수 있는 인계 문서 | 목표·현재상태·실행명령·통과검사·남은문제·다음행동·주의사항 7항목 작성 | 7항목이 한 페이지에 보인다 — [HANDOFF.md](HANDOFF.md) |
| 4 | AI B가 이어서 완성 | 새 세션 AI B(Codex)가 저장소+HANDOFF.md만으로 남은 검사 4~10 확인, 오너가 실제 클릭으로 최종 확인 | AI B 변경과 현재 동작이 같은 화면에서 연결되어 보인다 |
| 5 | 결과를 가리고 비교 | 두 AI 기록 비교표 + 앞으로의 선택 기준 3개 확정 | 비교표와 선택 기준이 같은 단위로 보인다 |
