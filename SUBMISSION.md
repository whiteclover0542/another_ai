# T05 제출 문서 — AI가 바뀌어도 작업 이어가기

> 이 문서는 PDF 변환용 최종 제출 문서입니다. 작업 과정의 전체 기록은 `PROGRESS.md`, 과제 원문은 `ASSIGNMENT.md`, 인계 문서는 `HANDOFF.md`를 참고하세요.

---

## 0. 과제 요약

- **과제**: 한 AI가 중단되거나 다른 AI로 바뀌어도, 저장소와 인계 문서만으로 같은 작업을 이어갈 수 있는지 확인하는 실험.
- **기반 앱**: "오늘의 CVE 정보판" (정적 사이트, HTML/CSS/JS)
- **추가한 기능**: 대표 CVE 목록 **심각도 필터** — 필터 버튼(전체/심각/높음/중간/낮음)을 클릭하면 목록이 그 심각도 항목만 즉시 보이도록 바뀐다.
- **공개 주소**: https://whiteclover0542.github.io/another_ai/
- **결과**: 검사 10개 중 10개 통과 (오너가 배포 주소에서 직접 클릭해 최종 확인, 2026-08-24)

---

## 1. 최종 제출물 ① — 검증 안내서

### 어디로 가나요
https://whiteclover0542.github.io/another_ai/

### 무엇을 하나요
1. "🎯 오늘의 대표 CVE" 카드에서 필터 버튼(전체/심각/높음/중간/낮음) 중 하나(예: "높음")를 클릭한다.
2. 목록이 그 심각도 항목만 남는지 확인한다.
3. "전체"를 다시 클릭해 전체 목록으로 돌아오는지 확인한다.

### 무엇이 보이면 통과인가요
클릭한 심각도 버튼이 강조 표시되고, 목록이 그 심각도 항목만(또는 0건이면 "해당 심각도의 대표 CVE 없음" 문구) 즉시 바뀐다. 위쪽 심각도 분포 도넛 차트·범례 숫자는 필터와 상관없이 항상 그대로다.

### 안 될 때
- 필터 버튼이 하나도 안 보이면: "🎯 오늘의 대표 CVE" 카드 자체가 접혔거나 오늘 기록에 대표 CVE가 없는 날일 수 있다 — 화면 아래 "날짜별 기록" 표에서 가장 최근 날짜에 값이 있는지 먼저 확인한다.
- 클릭해도 목록이 안 바뀌면: 브라우저를 강력 새로고침(캐시 무시, Ctrl+Shift+R)한 뒤 다시 시도한다.

---

## 2. 최종 제출물 ② — AI 3줄

- **AI에게 맡긴 일**: 개선 기능 설계(심각도 필터 정의·검사 10개 확정), 실제 구현(`index.html`/`assets/app.js`/`assets/style.css`), 문서 작성(ASSIGNMENT.md/PROGRESS.md/HANDOFF.md), 배포 상태 정적 검증.
- **내가 판단한 일**: AI가 처음엔 원본 today_information 저장소를 바로 고치자고 제안했지만 원본을 보존하는 방식을 선택했고, push 시점·GitHub Pages 설정·AI B로 어떤 도구(Codex)를 쓸지·최종 실제 클릭 검증은 전부 직접 결정하고 수행했다.
- **AI 말을 안 들은 일**: "이전 프로젝트는 놔두고 여기서 새로 작업할 순 없어?"라고 되물어, AI가 제안한 "원본 저장소에서 바로 개선" 방향을 거부하고 복사본으로 작업하는 방식으로 바꿨다.

---

## 3. 과제 카드 5개 한눈에 보기

| # | 카드 | 이번 실험에서의 구체 행동 | 통과 기준 |
|---|---|---|---|
| 1 | 같은 기준과 예산 정하기 | today_information 앱을 이 저장소로 복사, 심각도 필터 기능 추가 결정, 검사 10개·AI 상한(각 12회) 확정 | 기능 이름·사용법이 화면에 보이고 한 번 조작하면 결과가 바뀐다 |
| 2 | AI A와 제한 안에서 작업 | AI A(Claude Code)가 필터 구현·배포, 요청 8/12에서 인계로 전환 | AI A 멈춘 지점과 남은 일이 화면에 구분되어 보인다 |
| 3 | 사람이 읽을 수 있는 인계 문서 | 목표·현재 상태·실행 명령·통과 검사·남은 문제·다음 행동·주의 사항 7항목 작성(HANDOFF.md) | 7항목이 한 페이지에 보인다 |
| 4 | AI B가 이어서 완성 | 새 세션 AI B(Codex)가 저장소+HANDOFF.md만으로 남은 검사 4~10 확인, 오너가 실제 클릭으로 최종 확인 | AI B 변경과 현재 동작이 같은 화면에서 연결되어 보인다 |
| 5 | 결과를 가리고 비교 | 두 AI 기록 비교표 + 앞으로의 선택 기준 3개 확정 | 비교표와 선택 기준이 같은 단위로 보인다 |

### 두 AI 비교표

| 항목 | AI A (Claude Sonnet 5, Claude Code) | AI B (Codex 5.6 "Terra", medium) |
|---|---|---|
| 역할 | 기능 설계 + 처음부터 구현 + 배포 | 인계 문서만 보고 남은 검증 이어받기 |
| 총 요청(턴) 수 | 8 (상한 12) | 1 (자체 보고, 상한 12) |
| 총 경과 시간(분) | 기록 누락 | 5분 (자체 보고) |
| 통과 검사 수 (자체 검증 기준) | 실제 클릭 3 + 코드 추적 4 = 7/10 | 7/10 (정적 검증) |
| 실제 클릭 검증 도구 보유 | 없음 | 없음 |
| 남긴 오류 수 | 0 | 0 |
| 재작업량 (인계 이해 오류) | 해당 없음 | 0 |

**앞으로의 선택 기준**
1. 기능을 처음부터 설계·구현할 때는 AI A(Claude Code)부터 쓰고, 구현이 끝난 뒤 "인계 문서만 보고 검증·마무리"하는 단계에서는 다른 AI로 바꿔도 손해가 없다.
2. 인계 문서에 파일 경로·검사 번호·이유를 구체적으로 적으면 AI를 완전히 바꿔도 이해 오류·재작업이 0건이 된다 — 도구를 바꿀 계획이 있다면 인계 문서 작성에 공을 들인다.
3. "실제 화면을 클릭해서 확인"하는 마지막 단계는 AI 종류와 무관하게 사람이 직접 해야 한다 — 이 부분은 처음부터 사람 몫으로 계획한다.

---

## 4. 인계(HANDOFF) 방식 정리

AI A → AI B 인계는 **HANDOFF.md 한 파일**로만 이루어졌다. 이 대화의 전문이나 PROGRESS.md 전체는 AI B에게 넘기지 않았다.

### 4-1. 무엇을 넘겼나

- **저장소 링크**: `https://github.com/whiteclover0542/another_ai` (또는 로컬 클론)
- **딱 한 문장**: "HANDOFF.md를 읽고 거기 적힌 남은 검사 4~10번을 확인·완료해줘"
- 그 외 배경 설명이나 앞선 대화 내용은 일부러 주지 않았다 — AI B가 문서와 코드만으로 스스로 상황을 파악하는지가 이 카드의 검증 대상이었기 때문이다.

### 4-2. HANDOFF.md 구성 (7항목)

| 항목 | 내용 |
|---|---|
| ① 목표 | 심각도 필터 기능의 한 문장 정의 + 제약(필터는 목록에만 적용, 도넛 차트는 그대로, 세션 메모리만 사용) |
| ② 현재 상태 | 배포 완료 여부, 오너가 직접 확인한 골든 패스, 아직 미확인인 검사 번호, 변경된 파일 목록, 관련 커밋 해시 |
| ③ 실행 명령 | 로컬 실행 방법(`npx serve .`), `?debug=1` 장애 시연 접속법, 배포본 캐시버스팅(`?v=`) 주의사항 |
| ④ 통과한 검사 | 검사 10개 각각의 상태(실제 클릭 확인 / 코드상 미확인)를 표로 요약 |
| ⑤ 남은 문제 | 실제 브라우저로 확인해야 할 검사 번호와, 그중 특히 확인 방법이 필요한 항목(예: 콘솔에서 `setHighlightFilter('nonsense')` 직접 실행) |
| ⑥ 다음 행동 | 실행 → 남은 검사 확인 → PROGRESS.md 카드 4·5 채우기 → 실패 시 최소 수정 후 회귀 재확인 → 커밋·push 순서 |
| ⑦ 건드리면 안 되는 부분 | 원본 today_information 저장소/배포 금지, `renderSeverity()`(도넛 차트) 변경 금지, `history.json` 수동 수정 금지, `?debug=1` 장애 시연 로직 변경 금지, ASSIGNMENT.md 동결 |

### 4-3. 결과

- AI B(Codex, 새 세션)가 HANDOFF.md만으로 목표·현재 상태·남은 검사 범위를 **이해 오류 없이** 파악했다 — 인계 문서를 다시 고칠 필요가 없었다.
- AI B는 남은 검사 4~10번을 코드/정적 대조로 확인했고(이 환경도 실제 브라우저 클릭 도구가 없었음), 이후 오너가 배포 주소에서 4~10번 전체를 직접 클릭해 최종 확인했다.
- 재작업(인계 문서 이해 오류로 다시 한 작업)은 **0건**.

> 이 실험에서 확인된 것: 인계 문서에 "왜(이유)"까지 적어두면(예: "renderSeverity는 건드리지 말 것 — 필터가 침투하면 검사 8이 깨진다") AI가 완전히 바뀌어도 맥락 복원 비용이 거의 들지 않는다.

---

## 5. 최종 검사 10개 재실행 결과

오너가 https://whiteclover0542.github.io/another_ai/ 에서 2026-08-24에 직접 클릭해 재실행.

| # | 검사 내용 | 통과 여부 |
|---|---|---|
| 1 | 첫 로드 시 전체 항목 + "전체" 선택 표시 | ✅ 통과 |
| 2 | "높음" 클릭 → HIGH만 남고 개수 즉시 변경 | ✅ 통과 |
| 3 | 다시 "전체" 클릭 → 전체 복원 | ✅ 통과 |
| 4 | 필터 변경 후 새로고침 → "전체"로 초기화 | ✅ 통과 |
| 5 | 대표 CVE 0건인 심각도 클릭 → "해당 심각도의 대표 CVE 없음" 표시 | ✅ 통과 |
| 6 | `?debug=1` API 실패 재현 상태에서 필터 클릭해도 콘솔 에러 없이 동작 | ✅ 통과 |
| 7 | 잘못된 필터 값(`setHighlightFilter('nonsense')`) → "전체"로 안전 처리 | ✅ 통과 |
| 8 | 도넛 차트·범례는 필터와 무관하게 항상 전체 분포 표시 | ✅ 통과 |
| 9 | 출처 링크·조회 시각·비교 카드 등 기존 기능 정상 | ✅ 통과 |
| 10 | `?debug=1` 장애 5종 버튼 + 마지막 정상값 유지가 그대로 동작 | ✅ 통과 |

**10/10 통과.**

---

## 6. 전체 학생 자체 점검 체크리스트

- [x] 개선 기능 한 가지와 검사 10개를 작업 시작 전에 확정했다.
- [x] AI별 사용 상한과 계획 중단 시점이 기록돼 있다.
- [x] 무료 한도를 일부러 소진하거나 개인 결제를 새로 하지 않았다. (AI A 8/12, AI B 1/12 — 둘 다 상한 안에서 종료)
- [x] AI A의 시간·요청·검사·오류 기록이 저장소 변경과 맞는다. (시간 기록 누락 사실을 그대로 밝힘 — 위조 없음)
- [x] 인계 문서의 7개 항목이 모두 채워져 있고 실행 명령이 작동한다.
- [x] 첫 대화 전문 없이 AI B가 저장소와 인계 문서만으로 작업을 재개했다. (Codex, 이전 대화 미노출)
- [x] AI B 작업 뒤 검사 10개와 기존 기능 회귀 검사가 모두 통과했다. (오너 실제 클릭 10/10)
- [x] 두 AI의 시간·요청·오류·재작업 기록이 같은 단위로 정리됐다.
- [x] 운영 기준이 실제 측정 기록과 연결된다.
- [x] 공개 화면·파일·인계 문서·제출 기록에 개인정보와 비밀값이 0건이다.
- [x] 검증 안내서에 어디로 가나요·무엇을 하나요·무엇이 보이면 통과인가요·안 될 때를 모두 적었다.
- [x] 무엇을 하나요는 3단계 이내이며 공개 주소에서 바로 실행할 수 있다.
- [x] AI에게 맡긴 일·내가 판단한 일·AI 말을 안 들은 일을 한 줄씩 적었다.

**13/13 완료.**
